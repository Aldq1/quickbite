<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="false"
        android:label="QuickBite"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
            Google Maps API Key.
            Add  MAPS_API_KEY=<your-key>  to local.properties, then sync.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- Food ordering UI with bottom navigation — primary launcher -->
        <activity
            android:name=".ui.FoodOrderingActivity"
            android:exported="true"
            android:theme="@style/Theme.QuickBite.FoodOrdering">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Restaurant menu screen -->
        <activity
            android:name=".ui.RestaurantMenuActivity"
            android:exported="false"
            android:theme="@style/Theme.QuickBite.FoodOrdering" />

        <!-- Compose-based activity — handles QR deep links and internal order flow -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <!-- Deep link: quickbite://order/{restaurantId}/{tableNumber} -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="quickbite" android:host="order" />
            </intent-filter>
        </activity>
    </application>
</manifest>
